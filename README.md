# Travel_Blog

MERN stack travel blog application.

## Structure
- `frontend/` - React frontend
- `backend/` - Node.js/Express backend

## Setup
1. Setup backend: `cd backend && npm install && npm start`
2. Setup frontend: `cd frontend && npm install && npm run dev`